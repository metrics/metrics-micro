{
  "name": "metrics-micro",
  "version": "0.0.1",
  "description": "The micro middleware for metrics",
  "license": "MIT",
  "repository": "metrics/metrics-micro",
  "main": "index.js",
  "author": {
    "name": "Tobias Lins",
    "email": "me@tobi.sh"
  },
  "engines": {
    "node": ">=6"
  },
  "files": ["dist", "index.js"],
  "scripts": {
    "build": "babel src -d dist",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "metrics-core": "^0.0.4",
    "socket.io-client": "^2.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.8",
    "babel-preset-stage-2": "^6.18.0"
  }
}
